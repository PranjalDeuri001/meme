/* eslint-disable react-refresh/only-export-components */

import { createContext } from "react";

// Context for sharing vehicle selection state between components
export const VehicleSelectionContext = createContext(null);

export default VehicleSelectionContext;